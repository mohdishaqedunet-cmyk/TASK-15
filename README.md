# TASK-15
Building a structured machine learning workflow begins with loading the dataset and clearly separating the input features from the target variable. The features represent the independent variables used for prediction, while the target variable is the outcome we want the model to learn. 
